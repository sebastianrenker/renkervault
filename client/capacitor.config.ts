import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.renkerindustries.renkervault',
  appName: 'RenkerVault',
  webDir: 'dist'
};

export default config;
